'use client';

/**
 * @deprecated This component is no longer used in V2 layout.
 * The waterfall now uses a standard table with WaterfallTable component.
 */

export function StageColumn() {
  return null;
}
