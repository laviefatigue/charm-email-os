'use client';

// Re-export from new component for backward compatibility
// Prefer importing RequestRevisionButton directly for new code
export { RequestRevisionButton, RequestRevisionButton as RegenerateButton } from './RequestRevisionButton';
export type { } from './RequestRevisionButton';
